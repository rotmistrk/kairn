[package]
name = "txv-widgets"
version = "0.1.0"
edition = "2021"
description = "Concrete View implementations — ready-to-use interactive TUI components"

[dependencies]
txv-core = { path = "../txv-core" }
ignore = "0.4"
log = "0.4"
