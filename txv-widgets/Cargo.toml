[package]
name = "txv-widgets"
version = "0.1.0"
edition = "2021"
description = "Reusable interactive TUI widgets built on txv"
license = "MIT"

[dependencies]
txv = { path = "../txv" }
crossterm = "0.28"
ignore = "0.4"

[dev-dependencies]
tempfile = "3"