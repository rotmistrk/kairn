# 003.01 — MCP Tier 1: Read-Only Visibility

## Objective

Fix and extend existing MCP read-only tools so Kiro has full visibility
into kairn's state.

## STATUS: Mostly implemented already

Existing tools in `src/mcp/tools.rs`:
- `list_tabs` — has `focused`, `modified`, `cursor`, `selection`, `order`, `path`, `tab_type` ✓
- `list_terminals` — has name→index mapping ✓
- `get_terminal_content` — supports lookup by name OR index ✓
- `get_todo_tree` — exists ✓
- `get_tab_content` — reads editor buffer content by tab name ✓
- `get_messages` — exists ✓

## Remaining work (audit & gap-fill)

- Verify `list_tabs` includes `tab_kind` (editor/terminal/results/etc.)
- Verify terminal name resolution works end-to-end (the original task mentioned a bug)
- Add any missing fields that Kiro needs for orchestration (e.g., `is_kiro` flag)
- Ensure snapshot is populated correctly for all tab types

## Tests

- Scenario test: verify all fields present in `list_tabs` response
- Scenario test: verify `get_terminal_content` works by both name and index

## Effort: ~1 hour (audit + fix gaps)
