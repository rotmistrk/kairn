# 003.04 — MCP Tier 4: LSP Passthrough

## Objective

Expose LSP capabilities to Kiro via MCP so it can navigate code semantically.

## NOTE: LSP client already exists

kairn already runs LSP clients per language with full request/response handling.
This tier wires MCP tools to existing LSP infrastructure.

## New tools

| Tool | Description |
|------|-------------|
| `goto_definition` | Returns location(s) for symbol at file:line:col |
| `find_references` | Returns all reference locations for symbol |
| `rename_symbol` | Workspace-wide rename (applies via editor) |
| `get_diagnostics` | Current diagnostics for file (or all files) |
| `get_completions` | Completion items at position |
| `hover` | Type/doc info at position |

## Implementation

- MCP tool sends request via `McpAction` + oneshot reply channel
- Main thread forwards to LSP client, LSP response sent back via oneshot
- Timeout: 10s for LSP responses
- Rename flow: LSP `textDocument/rename` → workspace edit → apply to buffers

## Files

- `src/mcp/tools_lsp.rs` — new file for LSP tool handlers
- `src/mcp/commands.rs` — new `McpAction` variants for LSP
- `src/handler.rs` — route MCP→LSP requests

## Tests

- Scenario test: `get_diagnostics` returns current errors
- Scenario test: `hover` returns type info (needs LSP server running)
- Unit test: timeout handling when LSP is unresponsive

## Effort: ~4-5 hours
